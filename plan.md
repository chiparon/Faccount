# plan

## 当前统领
- 项目目标：开发一个个人账目记录应用，覆盖收入、支出、转账三类资金变化。
- 首版定位：单用户本地账本，不做登录注册。
- 分类策略：单条交易绑定一个分类，分类支持父子层级。

## 阶段路线
- `阶段 0` 仓库初始化与规范文档落地。
- `阶段 1` 后端项目骨架、数据库连接、基础模型、迁移/初始化机制。
- `阶段 2` 账户、分类、流水的基础 CRUD 与业务校验。
- `阶段 3` 前端页面骨架与接口联调。
- `阶段 4` 围绕转账、筛选查询、可用性与体验继续迭代。

## 当前完成情况
- [x] 统领方案确认。
- [x] 根目录规范文档建立。
- [x] `backend/`、`frontend/`、`worklog/` 目录建立。
- [x] 后端 V1 骨架、数据模型、接口、验证逻辑建立。
- [x] 前端 V1 骨架、页面、API 封装建立。
- [x] 本地安装后端依赖并通过基础测试。
- [x] 安装前端依赖并通过 Vite 构建。
- [x] 确认 MySQL 8 服务已安装并运行。
- [x] 配置 `backend/.env` 并完成数据库初始化。
- [x] 启动后端与前端并完成账户、分类、支出流水基础联调。

## 下一步
- 继续优化列表展示、字段命名和输入体验。
- 补充收入、转账的完整页面级联调验证。
- 视需要清理本轮测试数据。

## beta0.3 待实施需求
- 增加流水记录修改功能：可重新编辑名称、金额、时间、分类、账户、备注等字段。
- 增加 Trash 垃圾池：账户、分类、流水删除先软删除进入 Trash，支持搜索和恢复。
- 流水列表支持缩放：可扩大到全页查看，也可恢复默认布局。
- 增加页内分页“统计”：先由 5 个候选分支/方案 `candi-beta0.3-stats-01` 至 `candi-beta0.3-stats-05` 讨论不同统计页风格，用户选型后实施。
- 增加分页页面“预算流转”：探索收入来源、账户流转、分类层级、流水明细之间的球形/星点层级视图；可能需要给流水增加“来源逻辑”字段。
- 增加微信与支付宝流水识别导入：读取 `pageref/` 下示例 CSV，进入导入缓存池，用户微调后压入流水池，并支持撤销导入。
- 增加流水账目子元素功能：单条流水可拆分多个子项，每个子项可独立记录名称、金额、分类、备注。
- 优化流水筛选排序：增加搜索框；金额、时间、属性/分类、账户均支持筛选和排序；大筛选器支持日期范围、金额范围、类型、账户、分类等复杂条件。
